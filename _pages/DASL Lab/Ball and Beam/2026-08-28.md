---
title: "Potentiometer and Servo Motor"
tags:
    - BNB
date: "2026-08-28"
bookmark: true
---
## 목표
기울어진 Beam 위를 굴러다니는 공의 위치를 Potentiometer 센서로 실시간 측정하고, 이후 이 데이터를 바탕으로 PID/MPC 제어로 서보모터를 조종해 공의 위치를 원하는 곳에 유지하는 것이 최종 목표이다.

일단 이 글에선 센서와 모터를 다룬다.

## Linear Potentiometer
Potentiometer는 가변저항을 의미한다. 이는 "저항 값을 임의로 바꿀 수 있는 전자부품"이라고 한다.   
여기서는 300mm 길이, 10kΩ인 Linear Potentiometer를 사용한다.

<figure style="margin: 0; text-align: center;">
    <img src="/assets/img/posts/BNB/01.png" width="600" height="400" />
    <figcaption>Linear Potentiometer</figcaption>
</figure>

<br>

<!-- 이 센서는 저항 범위 0~10kΩ, 전압 범위 0~5V를 갖는다. 오른쪽 끝에는 5V가, 왼쪽 끝에는 GND(0V)가 연결되어 있다.
공이 센서 위 특정 지점을 누르면, 그 지점을 기준으로 저항이 두 구간으로 나뉜다. 

\[공,오른쪽\] 구간의 저항과 \[왼쪽,공\] 구간의 저항이다. 공이 오른쪽에 가까울수록 \[공,오른쪽\] 구간은 짧아져 저항이 작아지고, 동시에 반대로 \[왼쪽,공\] 구간은 길어져 저항이 커진다. 

이렇게 공의 위치에 따라 두 구간의 저항 비율이 실시간으로 바뀌고, 이 비율이 그 지점(OUT 핀)에서 출력되는 전압값을 결정한다. 즉 저항 비율의 변화가 원인이고, 전압의 변화는 그 결과다. -->

이 센서는 저항 범위 0~10kΩ, 전압 범위 0~5V를 갖는다. 센서에 대해 쉽게 이해하자면, 공이 오른쪽으로 갈 수록 저항은 낮아지고, 전압은 커진다. 반대로 공이 왼쪽으로 갈 수록 저항은 높아지고, 전압은 작아진다.

즉, 저항이 낮아지므로 전압은 커지는 것이고, 저항이 높아지므로 전압이 작아지는 것이다.
결론은 얻어낸 전압값으로 우리는 공의 위치를 파악할 수 있다.

<figure style="margin: 0; text-align: center;">
    <img src="/assets/img/posts/BNB/02.png" width="600" height="400" />
    <figcaption>Linear Potentiometer</figcaption>
</figure>

<br>

현재 이해해야 하는 함수는 `pot_read_position()`이다. 해당 함수는 Potentiometer와 연결된 A3핀의 전압을 공의 위치(m)로 변환해주는 함수이다.

```cpp
#define VCC 5.0                  // [V] SoftPot voltage source
#define L 0.3                    // [m] SoftPot length is 0.3 meters
#define METERS_PER_VOLT 0.06     // [m/V] Conversion constant

float pot_read_position(void) {
    // Reads SoftPot OUT value and converts it into a distance
    // Returns distance in [m]
    int raw;                     // [0-1023] 10-bit ADC value
    float volts;                 // [V] acquired voltage
    float ballPosition;          // [m] position on SoftPot
    int pinPot;                  // Nano Pin Name A3 (Pin #22) <-> SoftPot OUT
    
    pinPot = A3;
    raw = analogRead(pinPot);     // A3 핀의 전압을 0~1023 디지털값으로 읽음
    volts = (raw / 1023.0) * VCC; // [V] convert to volts (0.0 to 5.0)
    ballPosition = (VCC - volts) * METERS_PER_VOLT; // [m] calculate corresponding meters
    
    return ballPosition;
} // end pot_read_position
```

이제 Arduino의 `void loop()`에서 해당 코드가 실행되는데, `rDot`에 공의 속도를 저장하는 것을 볼 수 있다.

공식은 $속도 = 이동한 거리/시간$ 이다. 즉, `r`은 지금 위치, `rPrev`는 바로 이전 루프에서 측정한 위치다. 두 값의 차이를 샘플링 시간(`sTime_SEC`)으로 나누면 속도가 된다.


```cpp
// Step 4: Read ball position
r = pot_read_position();

// Step 5: Control law computations
rDot = (r - rPrev) / sTime_SEC; // ball's velocity
```

## Servo Motor
